#include "main.h"
/**
 * int _islower(int c) - checks if character is lowercase
* @c - character to check
* return on success 1
* return on failure 0
*/
int _islower(int c)
{
	int islower(int c);

	if (islower(c))
	{
		return (1);
	} else {
		return (0);
	}
}

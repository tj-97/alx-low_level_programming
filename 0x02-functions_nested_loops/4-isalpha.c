#include "main.h"
/**
 * main - entry point
 * _isalpha - checks if character is a letter
 *
 * return: always 0 (success)
 */
int main(void)
{
	int r;

	r = _isalpha(c);
	_putchar(r + '0');
	_putchar('\n');
	return (0);
}
